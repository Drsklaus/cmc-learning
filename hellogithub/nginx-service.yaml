apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-github
  name: nginx-service-fromgithub
  namespace: ns-bsbm-o-sre-playground
spec:
  selector:
    app: nginx-github
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
